{
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/exec": "^1.1.1",
    "@actions/glob": "^0.4.0",
    "@actions/io": "^1.1.3"
  },
  "scripts": {
    "save": "ncc build index.js -o dist/main --minify",
    "restore": "ncc build restore.js -o dist/restore --minify",
    "bangun": "npm run save && npm run restore"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.0"
  }
}
